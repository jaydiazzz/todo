const Service = appRequire( 'lib/TEMPLATE/service' ); // eslint-disable-line


describe( 'CAPITALIZEDTEMPLATE Service', () => {

	before( async ( ) => {


	} );

	describe( '#createCAPITALIZEDTEMPLATE', () => {
		context( 'Creating CAPITALIZEDTEMPLATE', () => {
			it( 'No test written', () => {

				expect( true ).to.equal( true );

			} );
		} );
	} );

	describe( '#updateCAPITALIZEDTEMPLATE', () => {
		context( 'Updating CAPITALIZEDTEMPLATE', () => {
			it( 'No test written', () => {

				expect( true ).to.equal( true );

			} );
		} );
	} );

	describe( '#deleteCAPITALIZEDTEMPLATE', () => {
		context( 'Deleting CAPITALIZEDTEMPLATE', () => {
			it( 'No test written', () => {

				expect( true ).to.equal( true );

			} );
		} );
	} );

	after( ( ) => {


	} );

} );
